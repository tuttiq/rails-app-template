%h1
  Usuários Tipo 1
%br

-if can? :manage, TypeOneUser
  %p
    =link_to 'Adicionar novo', new_type_one_user_path, :class => :btn

%table.table.table-striped.table-bordered.table-condensed
  %thead
    %tr
      %th
        Nome
      %th
        Nome de usuário
      %th
        E-mail
      -if can? :manage, TypeOneUser
        %th
          Ações

  %tbody
    - @type_one_users.each do |type_one_user|
      %tr
        %td
          =link_to type_one_user.name, type_one_user_path(type_one_user)
        %td
          =type_one_user.username
        %td
          =type_one_user.email
        -if can? :manage, TypeOneUser
          %td
            =link_to 'Editar', edit_type_one_user_path(type_one_user)
            |
            =link_to 'Remover', type_one_user_path(type_one_user), :method => :delete, :confirm => 'Tem certeza que deseja excluir?'
